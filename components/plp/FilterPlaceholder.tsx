export function FilterPlaceholder() {
  return (
    <div className="toolbar-card">
      <span className="toolbar-label">Filter placeholder</span>
      <p>
        Future facets can connect to search, inventory, delivery window, or merchandising rules without changing the page structure.
      </p>
    </div>
  );
}
